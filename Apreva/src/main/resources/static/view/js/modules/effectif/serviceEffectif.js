'use strict';
var effectifService = angular.module('effectifService', []);

effectif.service('serviceEffectif', function() {
	
	this.verifIBAN=function(ib)
			{
				return  false;
			}
	
});
